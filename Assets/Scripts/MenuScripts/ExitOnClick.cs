using UnityEngine;
using System.Collections;

public class ExitOnClick : MonoBehaviour {

	public void Quit(){
//this is for when we are running on the unity editor.
//		UnityEditor.EditorApplication.isPlaying = false;
		//if we want to do it differently on a web app (e.g. close tab) or something we'd do it differently.
	}
}
