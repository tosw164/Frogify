using System;

namespace POCC.Achievements
{
	/**
	 * Enum to represent the different achievement categories, used to 
	 * hold a count and be passed into different handler methods.
	 */ 
	public enum AchievementType
	{
		LEVEL=0, NPC_PERSUADED=1, COLLECTABLES=2, HIDDEN_ITEMS=3
	}
}

